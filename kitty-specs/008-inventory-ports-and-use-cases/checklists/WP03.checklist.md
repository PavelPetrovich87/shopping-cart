# Checklist: WP03 - Reservation Management

- [ ] `ReleaseStockReservation` implemented using `IStockRepository` and `IEventBus`
- [ ] `ConfirmStockDepletion` implemented using `IStockRepository` and `IEventBus`
- [ ] `ReleaseStockReservation` publishes `StockReleased` events
- [ ] `ConfirmStockDepletion` publishes `StockDepleted` events
- [ ] `Result` pattern used for all domain errors
- [ ] Unit tests for `ReleaseStockReservation` pass
- [ ] Unit tests for `ConfirmStockDepletion` pass
