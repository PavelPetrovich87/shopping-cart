# Checklist: WP02 - Core Use Cases

- [ ] `CheckStockAvailability` implemented using `IStockRepository`
- [ ] `ReserveStock` implemented using `IStockRepository` and `IEventBus`
- [ ] `ReserveStock` publishes `StockReserved` events
- [ ] `Result` pattern used for all domain errors
- [ ] Unit tests for `CheckStockAvailability` pass
- [ ] Unit tests for `ReserveStock` pass
